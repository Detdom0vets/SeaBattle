#ifndef GAME_H
#define GAME_H

#include "QLabel"
#include "utilities.h"
/*
class game
{
    int plshlf=10;                          //сколько кораблей у игрока
    int btshlf=10;                          //сколько кораблей у бота
    int lab=0;                              //сколько кораблей уже установлено
public:
    void PlayerShip();                      // -1 корабль у игрока

    void BotShip();                          // -1 корабль у бота

    bool AreShipsLeft();                    // Проверка остались ли корабли

    void CreateShip(int i,  int j, orientation in, int shiplenght);

    game();
    ~game();
    game& operator= (const game &Game);                                                            //перегрузка оператора присваивания
public slots:

signals:
    void oneshipdied();
};
*/
#endif // GAME_H
